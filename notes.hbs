<section class="hex-panneau {{#if actif}}actif{{/if}}" data-panneau="notes">
  <label class="hex-notes">
    <span>{{localize "HEXAGON.Champ.Notes"}}</span>
    <textarea name="system.notes" rows="16" placeholder="{{localize 'HEXAGON.Champ.NotesAide'}}" {{#unless editable}}disabled{{/unless}}>{{system.notes}}</textarea>
  </label>
</section>
